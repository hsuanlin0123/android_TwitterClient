<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.codepath.apps.restclienttemplate.TimelineActivity">
    <item android:id="@+id/acion_compose"
        android:title="composeAction"
        android:icon="@drawable/ic_compose"
        app:showAsAction="ifRoom"
        android:onClick="onClickComposeBtn"
        />
    <item android:id="@+id/acion_profile"
        android:title="profileAction"
        android:icon="@drawable/ic_profile"
        app:showAsAction="ifRoom"
        android:onClick="onClickProfileBtn"
        />


</menu>
